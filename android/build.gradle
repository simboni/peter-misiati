// Top-level build file. The Android Gradle Plugin version is pinned so CI builds
// are reproducible. AGP 8.5.x requires Gradle 8.7+ (see gradle/wrapper).
plugins {
    id 'com.android.application' version '8.5.2' apply false
}
